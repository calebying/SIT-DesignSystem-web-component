## Close

The close behaviour of `Sit-dropdown` can be modified using the `close` attribute

<Canvas of={DropdownStories.SitSelectClose}>
  <Story of={DropdownStories.SitSelectClose} />
</Canvas>

## Sit-select event

The `Sit-select` event is emitted on `Sit-dropdown` whenever a non-disabled item is clicked. Use `event.detail.item` to access the selected `SitDropdownItem` element.

Disabled items do not emit `Sit-select`.

<Canvas of={DropdownStories.SitSelectEvent}>
  <Story of={DropdownStories.SitSelectEvent} />
</Canvas>

## Sit Dropdown Item Customisation

Using Sit utility classes, the layout of `SitDropdownItem` can be customised as needed.

<Canvas of={DropdownStories.SitSelectDropdownItem}>
  <Story of={DropdownStories.SitSelectDropdownItem} />
</Canvas>