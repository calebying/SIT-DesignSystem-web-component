## Without actions

The `actions` slot is optional. When no elements are passed into it, Subnav renders the header and navigation items without the actions row, and the mobile toggle layout adjusts to account for its absence.

<Canvas of={SubnavStories.WithoutActions}>
  <Story of={SubnavStories.WithoutActions} />
</Canvas>
