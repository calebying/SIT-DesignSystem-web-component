import { Meta } from "@storybook/blocks";

<Meta title="Foundation/Layout" />

# Layout

Sit foundation styles include a responsive layout system that automatically adapts spacing across different screen sizes. All layout CSS custom properties scale at three primary breakpoints:

- **Mobile**: 0px and above (default)
- **Tablet**: 1024px and above
- **Desktop**: 1440px and above

## Key features

**Responsive spacing**: Layout gaps, padding, and component spacing adapt at each breakpoint to maintain optimal spacing hierarchy and visual rhythm across devices.

**Mobile-first approach**: The system uses progressive enhancement. Mobile styles are the default, then enhanced at larger breakpoints via CSS media queries.

## Layout and spacing variables

These variables enable responsive spacing and layout adjustments:

### Text gaps

Control spacing between text elements: `--Sit-text-gap-2-xs` through `--Sit-text-gap-2-xl`

### Layout gaps

Control spacing in layout components:

- Mobile: `--Sit-layout-gap-md: 1rem`
- Tablet (1024px+): `--Sit-layout-gap-md: 1.5rem`
- Desktop (1440px+): `--Sit-layout-gap-md: 2rem`

### Layout padding

Responsive padding for containers:

- Mobile: `--Sit-layout-padding-md: 1.5rem`
- Tablet (1024px+): `--Sit-layout-padding-md: 2rem`
- Desktop (1440px+): `--Sit-layout-padding-md: 3rem`

## How responsive layout works

All layout variables are defined at the root level with default (mobile) values, then updated via media queries at each breakpoint:

1. **Progressive enhancement**: Mobile styles load first, then enhance on larger screens
2. **Consistency**: All components using the same variables stay aligned
3. **Flexibility**: Override defaults by adjusting CSS variables in your own code
