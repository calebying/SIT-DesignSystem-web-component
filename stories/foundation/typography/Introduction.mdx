import { Meta } from "@storybook/blocks";

<Meta title="Foundation/Typography/Introduction" />

# Typography

Sit foundation styles include a comprehensive responsive typography system that automatically adapts font sizes and line heights across different screen sizes. All HTML elements (headings, paragraphs, labels, etc.) use CSS custom properties that scale at three primary breakpoints:

- **Mobile**: 0px and above (default)
- **Tablet**: 1024px and above
- **Desktop**: 1440px and above

## Key features

**Automatic responsive scaling**: All foundation elements automatically scale their typography based on the viewport size. No additional media queries needed. Import and use.

**Consistent typography hierarchy**: Headings (h1–h6) maintain visual hierarchy across all screen sizes with responsive font sizes and line heights:

- h1: 32px (mobile) → 36px (tablet) → 40px (desktop)
- h2: 28px (mobile) → 30px (tablet) → 32px (desktop)
- And so on for h3–h6

**Body text handling**:

- Small and regular body text (`body-sm`, `body-md`) remain consistent at 14px and 16px across all viewports
- Large body text (`body-lg`) scales from 18px (mobile) to 20px (tablet/desktop)

## Typography variables

Font sizes for headings, body text, labels, links and captions automatically scale at different breakpoints:

### Headings

- `--Sit-font-size-heading-sm`: Responsive H4 sizing
- `--Sit-font-size-heading-md`: Responsive H3 sizing
- `--Sit-font-size-heading-lg`: Responsive H2 sizing
- `--Sit-font-size-heading-xl`: Responsive H1 sizing

### Body text

- `--Sit-font-size-body-sm`: Small body text (14px on all viewports)
- `--Sit-font-size-body-md`: Regular body text (16px on all viewports)
- `--Sit-font-size-body-lg`: Large body text (responsive: 18px mobile → 20px tablet/desktop)

### Links

- `--Sit-font-size-link-xs`: 12px on all viewports
- `--Sit-font-size-link-sm`: 14px on all viewports
- `--Sit-font-size-link-md`: 16px on all viewports
- `--Sit-font-size-link-lg`: Responsive, 18px mobile → 20px tablet/desktop

### Line heights

Line height variables scale proportionally with font sizes for improved readability:

- `--Sit-line-height-xs` through `--Sit-line-height-3-xl`

Example progression:

- Mobile: `--Sit-line-height-xs: 24px`, `--Sit-line-height-md: 28px`
- Tablet (1024px+): `--Sit-line-height-xs: 24px`, `--Sit-line-height-md: 32px`
- Desktop (1440px+): `--Sit-line-height-xs: 24px`, `--Sit-line-height-md: 32px`

## How responsive typography works

All typography variables are defined at the root level with default (mobile) values, then updated via media queries at each breakpoint:

```css
:root {
  --Sit-font-size-heading-xl: var(--Sit-font-size-32); /* Mobile: 32px */
  --Sit-line-height-xl: var(--Sit-line-height-40);
}

@media screen and (min-width: 1024px) {
  :root {
    --Sit-font-size-heading-xl: var(--Sit-font-size-36); /* Tablet: 36px */
    --Sit-line-height-xl: var(--Sit-line-height-44);
  }
}

@media screen and (min-width: 1440px) {
  :root {
    --Sit-font-size-heading-xl: var(--Sit-font-size-40); /* Desktop: 40px */
    --Sit-line-height-xl: var(--Sit-line-height-48);
  }
}
```
